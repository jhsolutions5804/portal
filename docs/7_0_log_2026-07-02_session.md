# 2026-07-02 작업 요약 (춘식이 정리)

> 오늘 세션 전체 작업 로그. 운영 배포 완료분 / 테섭 반영분(운영 대기) / 미해결 이슈 / 다음 할 일 순.

---

## 1. 운영(portal) 배포 완료

### PJT 워크스페이스 (pjt · pjt_ph4)
- **주간 일정**: 좌우 화살표(2주 단위 이동) + `오늘` 버튼 추가
- **월간 캘린더**: 날짜별 일정 직접 등록 · 수정 · 삭제
- **월간 상단 `yyyy년 mm월` 클릭** → 원하는 시점으로 바로 점프(월 선택기)
- **근태 출역현황**: `전체 출역 체크/해제` 일괄 버튼(좌측 정렬) + `전체 공수 일괄 조정` 입력·적용(우측)
  - 일괄 저장은 Firestore 1회 쓰기로 처리(개별 반복 호출 안 함)

### 회계장부 (gihoek)
- 결제수단에 `외상거래` 추가 (법인카드·개인카드·현금·계좌이체·외상거래)
- 장부 등록창: 하단 바텀시트 → **화면 중앙 카드**로 변경
- 상세내역 **필터**: 날짜 오름/내림차순, 항목별, 거래처별 + 필터 초기화

### 포털 홈 (index.html)
- PJT 홈 레이아웃 1차: 컨테이너 폭 확대, 상단 PJT 카드 좌측 정렬

---

## 2. 테섭(portal-test) 반영 — 운영 배포 대기 (대표님 확인 필요)

- **PJT 홈 통합 캘린더**: 전체 PJT(P4 Ph2 + P4 Ph4) 일정을 월간 캘린더로 표시, 날짜 클릭 시 우측에 그 날 일정 + 그 날짜 업무지시·보고 함께 표시
  - 데이터: `user_schedules` + `ph4_schedules`(일정), `daily_report_docs` + `ph4_reports`(업무지시, 날짜가 문서 ID)
- **일정 등록자 = 로그인 계정 자동 반영** (pjt · pjt_ph4)
- **공사일보 페이지 portal 이관**: `daily-report/index.html` + `daily-report/data.js` 신규 생성 (기존 별도 저장소 `p4ph2-fab-workspace`에서 가져옴, Firebase 동일 프로젝트라 데이터 연결 유지). pjt·pjt_ph4의 공사일보 링크를 portal 내부 경로로 변경
- **공사일보 작성자 = 로그인 계정 자동 반영**
- **로그인 계정 공유 방식**: 포털 로그인 시 계정 이름을 브라우저 저장소(`jh_login_name` / `jh_login_full`)에 기록, 로그아웃 시 제거 → 같은 origin(jhsolutions5804.github.io) 하위 페이지들이 읽어서 등록자·작성자 자동 채움
- **PJT 홈 레이아웃 최종**(미리보기로 확정 후 반영):
  - 상단 카드 **가로 스크롤**(고정폭 460px, 화면 좁아져도 세로로 안 접힘)
  - 콘텐츠 **전체 폭**(여백 축소)
  - **달력 확대**(날짜칸 76px, 날짜 15px, 점 6px)

---

## 3. 확인 · 미해결 사항

- **⚠️ 테섭 Firebase 격리 미완성**: 현재 테섭(portal-test)이 테스트 프로젝트(`portal-test-6e0ff`)가 아니라 **운영 프로젝트(`p4ph2-fab-506a7`)를 사용 중**. 즉 테섭에서 일정·공사일보·근태를 입력하면 운영 데이터에 반영됨.
  - 대표님이 데이터 복사 스크립트 완료 (컬렉션 51 / 문서 352 복사).
  - **남은 단계**: 테섭 코드(index.html, pjt 등)의 Firebase config를 테스트 프로젝트로 교체.
- **GitHub Pages 빌드 지연**: 하루 종일 Pages 빌드가 정체·지연 반복. 코드는 저장소에 정상 반영되나 화면 반영이 느림. 마지막 카드 배치 커밋 빌드 대기 중. (코드/문법 문제 아님 — 인프라 지연)

---

## 4. 다음 할 일

1. 테섭에서 PJT 홈 캘린더 · 일정 등록자 자동 · 공사일보 이관/작성자 자동 · 카드 가로스크롤 레이아웃 최종 확인
2. 이상 없으면 **운영(portal) 배포**
3. **테섭 Firebase 격리 완성** (테섭 코드 config를 portal-test-6e0ff로 교체)

---

*작성: 춘식이 · 2026-07-02*
