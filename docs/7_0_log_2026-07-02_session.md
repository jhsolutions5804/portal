# 2026-07-02 작업 요약 (춘식이 정리 · 최종)

> 오늘 세션 전체 작업 로그. **운영(portal) 배포까지 전부 완료.**

---

## 1. 운영(portal) 배포 완료 — 전 항목

### PJT 워크스페이스 (pjt · pjt_ph4)
- 주간 일정: 좌우 화살표(2주 단위 이동) + `오늘` 버튼
- 월간 캘린더: 날짜별 일정 등록 · 수정 · 삭제
- 월간 상단 `yyyy년 mm월` 클릭 → 시점 점프(월 선택기)
- 근태 출역현황: `전체 출역 체크/해제`(좌) + `전체 공수 일괄 조정`(우)
- **일정 등록자 = 로그인 계정 자동 반영**
- 공사일보 링크 portal 내부 경로로 변경

### 회계장부 (gihoek)
- 결제수단 `외상거래` 추가 / 등록창 중앙정렬 / 상세내역 필터(날짜·항목·거래처)

### 포털 홈 (index.html)
- **PJT 홈 통합 캘린더**: 전체 PJT 일정 월간 표시 + 날짜 선택 시 그 날 일정·업무지시 표시
- **최종 레이아웃**: 상단 카드 가로 스크롤(고정폭 460px, 세로 안 접힘) + 콘텐츠 전체폭(여백 축소) + 달력 확대
- 로그인 계정 이름을 브라우저 저장소(jh_login_name/full)에 기록 → 하위 페이지가 등록자·작성자 자동 채움

### 공사일보 (신규: daily-report/)
- 별도 저장소(p4ph2-fab-workspace)에서 portal로 **이관** (index.html + data.js)
- 작성자 = 로그인 계정 자동 반영

### 인프라
- **`.nojekyll` 추가** (운영·테섭 양쪽): Jekyll 빌드 건너뛰기 → 빌드 실패·지연 해결

---

## 2. 테섭(portal-test) Firebase 격리 완료
- 전 모듈(index.html·pjt·pjt_ph4·gihoek·hr·edoc·daily-report)이 테스트 프로젝트(`portal-test-6e0ff`) 사용
- 운영 데이터(`p4ph2-fab-506a7`)와 완전 분리 — 테섭 입력이 운영에 영향 없음
- 테스트 데이터 51컬렉션/352문서 복사 완료(대표님), 데이터 정상 확인

---

## 3. 주요 트러블슈팅 기록
- **GitHub Pages 빌드 반복 실패**: 원인은 `.nojekyll` 부재로 인한 Jekyll 빌드 오류. 하루 종일 화면 미반영의 진짜 원인. `.nojekyll` 추가로 해결(빌드 22초로 단축).
- **PJT 홈 캘린더 격자 깨짐**: 미디어쿼리/클래스 대신 요소 인라인 스타일로 격자 고정하여 해결.

---

## 4. 남은 참고사항
- 운영 배포분은 Pages 빌드 완료 후 화면 반영(진행 중).
- 운영은 운영 Firebase(p4ph2-fab-506a7) 유지, 테스트 설정 혼입 없음 확인 완료.

---

*작성: 춘식이 · 2026-07-02 (최종)*
