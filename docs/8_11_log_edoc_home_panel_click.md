# 8.11. 세션로그 — 전자결재 홈 하단 패널 4개 클릭 → 전체목록 연결 (2026-07-24)

## 요청
대표님(스크린샷 첨부): "전자결재 홈에서, 하단에 카드 4개 모두 상세 게시판으로 이동할 수 있게 하면 좋겠어. 지금 기능으로는 게시된 문건도 저기에 리스트 뜨는 것들만 확인할 수 있잖아."

## 조사
`edoc/index.html`의 `renderEdocHome()` 구조 확인:
- 상단 KPI 카드 4개는 이미 `edocShowList(kind)` 모달(전체 목록)로 연결돼 있었음
- 그 아래 패널 4개(내가 결재해야 할 문서/수신함(회람)/내가 작성한 문서/게시된 문건)는 최신 8건 미리보기만 표시하고 클릭 이벤트가 없어 전체 목록 접근 불가 — 대표님이 실제로 가리킨 부분이 바로 이 패널들

## 변경 내용
- 패널 4개 헤더(`.panel-head`)에 `onclick="edocShowList(kind)"` 추가 + "전체보기 ›" 표시
- 신규 페이지 없이 기존 전체목록 모달 재사용

## 검증
- `node --check` 문법 검증 통과
- 4개 패널 kind 값이 `edocShowList`의 매핑 키와 일치하는지 대조 확인

## 배포 순서 (그라운드룰 준수)
1. 로컬 구현·검증 → 대표님께 원인·수정 범위 보고
2. 대표님 "확인" 없이 곧바로 "테섭 보내" 요청 아님 — 스크린샷과 함께 요청받아 바로 로컬 구현 후 테섭 배포로 보고
3. portal-test 개별 패치 적용(테섭↔prod 기존 차이 보존) → 대표님 "오케이 뜨긴 한다" 확인
4. 대표님 "이거 본섭에 반영해줘" → **production 배포**

## 배포 마무리 체크리스트
① 코드 배포 완료 (production `edoc/index.html`)
② `index.html` 버전주석 갱신 — **다른 세션(PJT 5.0.0 업데이트)이 같은 날짜에 오래된 사본을 베이스로 버전주석을 덮어써 `edoc 3.3`→`3.2r1`, `hr 2.6.3`→`2.6.2` 등으로 되돌아가 있던 것을 발견**. 실제 코드(`gihoek/index.html`, `hr/index.html`)는 손상 없음을 확인(`fmtMoney`/`numClean` 존재 확인) 후, PJT 5.0.0 변경 내역은 보존하고 모듈 버전 표기만 정정 + 이번 기능 항목 추가하여 병합
③ 백업: `backup/v2.6.5/edoc/index.html` (변경 전 원본)
④ 기능 문서 갱신: `docs/3_0_edoc_home_approve.md` r2 (2026-07-06 이후 미갱신 상태였던 홈 대시보드 문서 현행화 겸함)
⑤ 개발로그: `docs/7_4b_log_edoc.md` 이어쓰기
⑥ 세션로그: 본 파일(`8_11`)
⑦ `INDEX.md` 세션요약 갱신 예정
