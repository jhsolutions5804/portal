# 세션 로그 — 2026-09-16 (이어서 2) — 근로자 명부 렌더 버그 수정 + 퇴사자 필터

> 작성: 춘식이(Claude)

## 요청

대표님 (스크린샷 2장) — ① 근로자 명부에서 퇴사자 따로 볼 수 있게 해달라. ② 뭔가 수정하고 나면 명부 화면이 이상한(구버전) 레이아웃으로 바뀌는 버그 수정, 항상 스샷1 레이아웃으로 보이게.

## 진단

`hr/index.html`에 `renderRosterMain`이 두 번 정의되어 있었고(뒤 정의만 유효), 정작 모든 수정/삭제/퇴직처리 액션은 완전 별도의 구버전 함수 `renderRosterList`(6열 카드형, 스샷2)를 호출해 화면을 갱신하던 것이 원인. 명부 화면 자체 버그가 아니라 "갱신 시 호출하는 함수가 레거시를 가리키던" 문제.

## 수정

죽은 첫 번째 `renderRosterMain` 정의 및 구버전 `renderRosterList` 함수 전체 삭제, 모든 갱신 호출을 `renderRosterMain()`으로 통일. 상단에 재직자/퇴사자 필터 탭 추가(기본값 재직자, 인원수 표시, 퇴사자 탭엔 퇴직일 배지). 상세: `7_36_log_hr_roster_render_fix.md`.

## 배포

대표님 지시("동시에 배포해") 따라 production + portal-test 동시 배포.

1. 로컬 검증: 문자열 치환 매칭 수 확인, `renderRosterList` 완전 제거 확인, node --check 통과, 설정 혼입 없음 확인
2. 백업: `backup/v2.4.0/hr/index.html` (양쪽)
3. 배포: `hr/index.html` (양쪽), 버전 2.3.0 → 2.4.0
4. Pages 강제 재빌드 → 양쪽 `built` 확인
5. 문서 갱신: `2_1_hr_roster_r5.md` (r7)

## 남은 이슈

없음. 대표님 실제 화면 확인 필요 (수정 후 화면 유지되는지, 퇴사자 탭 정상 동작하는지).
