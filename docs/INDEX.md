# JH Solutions 포털 — 문서 인덱스

> 최초 작성: 2026-06-26 · 최종 수정: 2026-07-20(r2) · 전자결재 업무일지 임시저장/반려 상태 수정 기능 추가 · 작성: 춘식이(Claude)

---

## 0. 기반 문서

| 파일명 | 내용 |
|--------|------|
| `0_ground_rule_r9.md` | Ground Rule, 용어 정의, 업데이트 절차, Semantic Versioning |
| `0_update_algorithm.md` | 업데이트 알고리즘 상세 절차 |
| `0_1_portal_concept.md` | 포털 concept, structure |
| `0_2a_portal_rule_r2.md` · `0_2b_portal_rule_r2.md` | 포털 기본 rule (a/b 분할) |
| `0_3_portal_auth_r3.md` | 포털 인증 구조 (+ 모바일 로그인 게이트 r3) |
| `0_4_structure_index.md` | 구조 인덱스 (전체 앱·컬렉션 목록) |

---

## 1. 기획

| 파일명 | 내용 |
|--------|------|
| `1_0_gihoek_home_r2.md` | 기획 홈 |
| `1_1_gihoek_project.md` | 프로젝트 |
| `1_2_gihoek_company.md` | 거래처 |
| `1_3_gihoek_estimate_r3.md` | 견적 |
| `1_4_gihoek_settle.md` | 정산 |
| `1_5_gihoek_account.md` | 회계 (외상거래·상세내역 필터·등록창 중앙정렬 r2) |

---

## 2. 인사

| 파일명 | 내용 |
|--------|------|
| `2_0_hr_home_r3.md` ~ `2_7_hr_leave_status_r2.md` | 인사 각 기능 (홈·명부·노무연간·연차·연장·급여명세·퇴직·휴가현황) |

---

## 3. 전자결재

| 파일명 | 내용 |
|--------|------|
| `3_0_edoc_home_approve.md` · `3_1_edoc_daily_leave.md` · `3_3_edoc_docs.md` | 전자결재 각 기능 |
| `3_4_edoc_overtime_r1.md` | **초과근로 결재** (통상임금 자동계산·결재선·승인 시 인사 자동연동) |

---

## 4. PJT 관리

| 파일명 | 내용 |
|--------|------|
| `4_0_pjt_home.md` | PJT 홈 — **통합 캘린더 레이아웃 4.4.0** (카드 가로스크롤·전체폭·달력, 날짜별 일정+업무지시) |
| `4_1_pjt_fab.md` | P4 Ph2 FAB — 홈·오늘·주간(좌우이동)·캘린더(등록/수정·년월점프)·등록자 자동 (4.4.0) |
| `4_1_pjt_fab_attend_progress.md` | P4 Ph2 FAB — 근태(출역·공수 일괄 4.4.0)·공정 · **공정표 PPT 상세 6P·설치진행도·섹터공정율(4.5.1)** |
| `4_2_pjt_sup.md` | P4 Ph4 SUP — FAB 동일 반영 |

---

## 5. 조직도 / 6. Portal 관리

| 파일명 | 내용 |
|--------|------|
| `5_org_chart.md` | 조직도 |
| `6_portal_admin_r2.md` | Portal 관리 |
| `6_expense_upload_log.md` · `expense_upload_routine.md` | 경비 업로드 로그·루틴 |

---

## 7. 개발 로그

| 파일명 | 내용 |
|--------|------|
| `7_0_log_2026-07-02_session.md` | **2026-07-02 세션 종합 로그** (운영 배포·Firebase 격리·트러블슈팅) |
| `7_1_log_portal_r4a.md` ~ `7_7_log_mobile_r1.md` | 모듈별 개발 로그 (portal·gihoek·hr·edoc·pjt·조직/portal관리·모바일통합) |
| `7_8_log_2026-07-06_session.md` | **2026-07-06 세션 로그** (모바일 전자결재 작성/결재·PC 초과근로 결재·로그인 게이트·다수 버그수정) |

---

## 8. 모바일 (m/)

| 문서 | 내용 |
|---|---|
| `8_0_mobile_r2.md` | 모바일 UI 통합 (r2) — UA 분기, m/ 앱 7종, PJT(공정·일정·근태·공수·06시), 인사 명단 우선 재구성, 업무지시/보고 구조 |
| `8_1_mobile_edoc_r1.md` | **모바일 전자결재** — 결재(승인/반려·관리자 대행)·작성 3종(업무일지/연차/초과근로)·삭제 |
| `8_6_log_edoc_daily_edit.md` | **전자결재 업무일지 임시저장/반려 상태 수정 기능** — 상세화면 수정 버튼, 기존 문서 덮어쓰기 |

---

## 세션 요약 — 2026-07-06 (r11 · v2.1.0)

**모바일 전자결재 완성 + PC 초과근로 결재 + 로그인 게이트.** 모바일(`m/edoc.html`) 결재(승인/반려·관리자 대행)·작성 3종(업무일지·연차·초과근로)·삭제. PC(`edoc/index.html`) 초과근로 결재(통상임금 자동계산·결재선 김종화 차장/김민서 대리·승인 시 인사 `overtime` 자동연동). PJT(`m/pjt.html`) 일정 등록/완료/수정·공수 입력·06시 기준. 인사(`m/hr.html`) 모든 메뉴 근로자 명단 우선→클릭 시 해당 문서, 근로자명부·퇴직금 초과근로 미표시. 로그인 게이트(`m/auth-guard.js`·`index.html`): 로그인 정보 없이 서브페이지 직접 진입 차단→메인 유도·복귀, 무한루프 방지, splash fallback. 버그: 승인완료 결재버튼 숨김·퇴직원서 타일 배치·06시 통일. 백업 `backup/v2.1.0/`. 상세는 `7_8_log_2026-07-06_session.md`.

---

## 세션 요약 — 2026-07-04 (r10)

**모바일 UI 통합 (v2.0.0)**. PC/모바일 UA 기준 분기(PC 무변경) + `m/` 앱 7종(home·gihoek·hr·edoc·admin·account·pjt). PJT 모바일: 공정 실연동(FAB/SUP, calcZone PC값 일치), 일정 캘린더(오늘 자동선택·날짜별 일정+업무지시/보고), 근태(날짜이동·출역토글). 공정 시작일 `pjt_settings`에서 자동조회. **업무지시/보고 경로 정정**: `daily_report_docs`→실제 `daily_reports_{날짜}`/`ph4_daily_{날짜}`(text/name/time/ts). `preview/` 정리. 백업 `backup/v2.0.0/`. **미진행**: 하루 넘기는 일정 완료체크 단일화(→ `7_7_log_mobile_r1.md`).

---

## 세션 요약 — 2026-07-02 (r7)

**PJT 홈 캘린더 개편 + 로그인 자동반영 + 공사일보 이관 + Firebase 격리 완료 (테섭 검증 → 운영 배포 완료)**

| 영역 | 버전 | 주요 변경 |
|------|------|-----------|
| PJT 관리 | 4.3.0 → **4.4.0** | 포털 PJT홈 통합 캘린더(카드 가로스크롤·전체폭·달력확대, 날짜별 일정+업무지시), 주간 좌우 2주 이동, 월간 일정 CRUD·년월 점프, 근태 출역/공수 일괄, 일정 등록자 로그인 자동반영 |
| 회계 | 1.5.x → **r2** | 결제수단 외상거래, 상세내역 필터(날짜·항목·거래처), 등록창 중앙정렬 |
| 공사일보 | 신규 이관 | 별도 저장소(p4ph2-fab-workspace) → `portal/daily-report/` 편입, 작성자 로그인 자동반영 |
| 인프라 | — | **테섭 Firebase 격리 완료**(portal-test-6e0ff 전환), **`.nojekyll` 추가**(빌드 실패·지연 해결), 로그인 계정 localStorage 공유 |

**운영 배포**: index.html·pjt·pjt_ph4·daily-report(+data.js)·.nojekyll — 운영 Firebase(p4ph2-fab-506a7) 유지, 테스트 설정 혼입 없음 확인.

**주요 트러블슈팅**: GitHub Pages 빌드 반복 실패(원인=`.nojekyll` 부재로 인한 Jekyll 오류) → `.nojekyll`로 해결. 캘린더 격자 미표시 → 요소 인라인 스타일로 격자 고정.

**상세**: `7_0_log_2026-07-02_session.md`

---

## 세션 요약 — 2026-07-03 (r9)

**공정표 PPT 상세 6P 신설 + 지급내역 직접수정 (테섭 검증 → 운영 배포 완료)**

| 영역 | 버전 | 주요 변경 |
|------|------|-----------|
| 공정표 PPT | 4.5.0 → **4.5.1** | 섹터별 상세 6P(전일/금일/누계/증감), 설치 진행도 `instBarPct` 반영(2p와 동일), 헤더 섹터 전체 공정율(+전일대비) |
| 급여명세서 | r3 → **r4** | 지급내역 직접 수정(공제와 동일 오버라이드, PC 전용) |
| 백업 | v1.2.0 → **v1.3.0** | pjt·hr index.html |
| 인사 문서 | 2_0 r3a | 구 정산도우미 가이드 대조 → portal 문서 유일 누락분(PDF 출력 규격) 보완, administrator 삭제 가능 상태 |

**운영 커밋**: pjt `16e9ada9`·`285f6ee1` / hr `8bb7b1c0`

---

## 세션 요약 — 2026-07-08 (r1)

**공사일보 조회 수정/삭제 기능 (테섭 검증 → 운영 배포 완료)**

| 영역 | 버전 | 주요 변경 |
|------|------|-----------|
| PJT 공사일보 | **8_2 r1** | 조회 상세 모달에 수정/삭제 추가(FAB·SUP). merge 갱신, confirm 삭제, XSS 이스케이프 |
| 백업 | v2.1.0 → **v2.2.0** | pjt·pjt_ph4 index.html |

**운영 커밋**: pjt `a0942203` / pjt_ph4 `18febd57`
**검증**: node --check + jsdom 흐름 FAB 19/19·SUP 19/19, prod/test diff 코드 동일 확인

---

## 세션 요약 — 2026-07-08 (r2)

**홈 일정 상세/등록/수정/삭제 (테섭 검증 → 운영 배포 완료)**

| 영역 | 버전 | 주요 변경 |
|------|------|-----------|
| portal 홈 | **8_3 r1** | 우측 상세일정 클릭→상세 모달, 홈에서 일정 등록/수정/삭제. PJT앱과 동일 필드로 저장(양방향 호환) |
| 백업 | v2.2.0 → **v2.3.0** | index.html |

**운영 커밋**: index `ae103662` (test `5b847db2`)
**검증**: node --check + jsdom 흐름 prod·test 각 29/29, 본섭 안전성 검토(식별자/스코프/캐시파급/z-index/모바일) 통과


---

## 세션 요약 — 2026-07-14 (r1)

**공사일보 작성자 드롭다운 버그 수정 (urgent hotfix, production 직접 배포)**

| 영역 | 버전 | 주요 변경 |
|------|------|-----------|
| PJT 공사일보(standalone) | **8_4 r1** | 작성자 드롭다운 비어있던 버그 수정 — standalone 창 Auth 미존재로 Firestore 조회 실패해도 로그인 계정 항상 추가+자동선택 |
| PJT 문서 | 4_1·4_2 → **4.5.1** | FAB·SUP 문서에 원인·수정 내역 반영 |
| 백업 | v2.3.0 → **v2.4.0** | daily-report/index.html · ph4.html |

**운영 커밋**: daily-report/index.html `b1d29403` / daily-report/ph4.html `be34b077` / index.html(ver 주석) `252237b4`
**검증**: node --check + jsdom 3시나리오(정상조회/조회실패/빈컬렉션) 전부 통과
**비고**: 대표님 실사용 중 차단 이슈로 테스트서버 단계 생략, 명시 승인 하 production 직접 배포


---

## 세션 요약 — 2026-07-20 (r1)

**PJT 일정 자정 넘기는 일정 시작일 표시 (테섭 검증 → 대표님 확인 → 운영 배포 완료)**

| 영역 | 버전 | 주요 변경 |
|------|------|-----------|
| PJT 일정(FAB·SUP) | **8_5 r1** | "저녁~다음날 새벽"류 자정 넘기는 일정은 시작일에만 표시(다음날 중복 노출 제거). 진짜 다중일 일정(출장 등)은 기존대로 유지. 상세·수정에는 원본 종료일·시간 보존 |
| PJT 문서 | 4_1·4_2 → **4.5.2** | FAB·SUP 문서에 판별 로직·검증 내역 반영 |
| 백업 | v2.4.0 → **v2.5.0** | pjt/index.html · pjt_ph4/index.html |

**배포 순서**: portal-test 선배포 → 대표님 실사용 확인("테섭에 문제 없네") → production 배포
**검증**: node --check + 단위 테스트 6시나리오(자정넘김/다중일/컨퍼런스/엣지/단일일/Todo) 전부 통과

---

## 세션 요약 — 2026-07-20 (r2)

**전자결재 업무일지 임시저장/반려 상태 수정 기능 추가 (테섭 검증 → 대표님 확인 → 운영 배포 완료)**

| 영역 | 버전 | 주요 변경 |
|------|------|-----------|
| 전자결재 업무일지 | **3.1 → r1** | 회수 후 임시저장(draft)/반려(rejected) 상태에서 "✏️ 수정" 버튼 노출(`renderDailyDetail`), 기존 문서 값 미리채움 수정모드(`renderDailyWrite(existingDoc)`), 저장 시 신규생성 대신 기존문서 덮어쓰기(`dailySave`→`setDoc merge`, 중복 문서 방지) |
| 백업 | v2.5.0 → **v2.6.0** | `backup/v2.6.0/edoc/index.html` |

**배포 순서**: portal-test 선배포 → 대표님 실사용 확인("ㅇㅋ") → production 배포
**검증**: node --check 전체 script 문법 통과, 기존 무인자 작성 흐름 하위호환 확인, 특수문자 이스케이프 확인
**운영 커밋**: `edoc/index.html` `86e4caaf` / `index.html`(버전주석)
**상세**: `8_6_log_edoc_daily_edit.md`

---

## 세션 요약 — 2026-07-21 (r1)

**초과근로 상신/입력에 사유(reason) 필드 추가 (테섭 검증 → 대표님 확인 → 운영 배포 완료)**

| 영역 | 버전 | 주요 변경 |
|------|------|-----------|
| 전자결재 초과근로 | **3.4 → r2** | "초과근로 작성" 상신 폼에 사유 텍스트영역 추가, `edoc_overtime` 저장, 상세보기 노출, 승인 시 인사 `overtime` 자동연동에도 사유 함께 전달 |
| 인사 초과근로 | **2.4 → r3** | PC/모바일 입력·수정 폼에 사유(선택) 필드 추가, 상세/조회 테이블에 사유 컬럼 추가. 문서 내 스키마 오기(`pay`·`hourlyWage`) 정정(`amount`·`rate`가 실제 사용 필드) |
| 백업 | v2.6.0 → **v2.6.1** | `backup/v2.6.1/hr/index.html`, `backup/v2.6.1/edoc/index.html` |

**경위**: 최초 요청("초과근로 상신 시 사유 작성") 대상을 인사 앱 직접입력 탭으로 오인 → 스크린샷으로 실제 화면(전자결재 상신 폼) 확인 후 정정, 양쪽 모두 반영
**배포 순서**: portal-test 선배포 → 대표님 확인("확인됐어") → production 배포
**검증**: node --check (hr 비-모듈 / edoc 모듈 `.mjs` 추출) 각각 문법 통과
**운영 커밋**: `hr/index.html` `bc130cd` / `edoc/index.html` `820f134` / `index.html`(버전주석) `5ed6897`
**상세**: `8_7_log_overtime_reason.md`
**미해결**: `hr/2_4_hr_overtime_r2.md`(중복 문서, `hr/` 폴더 내 위치) 정리 필요 — 정식 위치는 `docs/`

---

## 세션 요약 — 2026-07-21 (r2)

**`hr/` 폴더 내 중복 문서 정리 (코드 변경 없음, 문서 전용)**

- 대표님 질문("hr에도 초과근로, edoc에도 초과근로 있는데 1:1 매칭 아니냐")을 계기로 `hr/2_4_hr_overtime_r2.md` ↔ `docs/2_4_hr_overtime_r2.md` 중복 여부를 커밋 이력으로 전수 추적
- 결론: `hr/` 폴더 안 2개 파일(`2_4_hr_overtime_r2.md`, `7_3_log_hr_r5.md`)은 2026-06-26~27, `docs/` 폴더 중앙집중 체계로 넘어가기 전(2026-06-29 분할 기점) 코드 옆에 만들어둔 **초기 사본**이며 이후 방치됨. 전자결재(`docs/3_4_edoc_overtime_r1.md`)와는 무관 — 이미 별도 문서로 정확히 분리되어 있었음
- 두 파일에만 있던 고유 내용(초과근로 PC 레이아웃 다이어그램·함수표, 2026-06-24~27 초기 커밋 이력)을 `docs/7_9_archive_hr_duplicate_cleanup.md`로 보존 후 `hr/` 폴더의 두 파일 삭제

**변경 파일**: `docs/7_9_archive_hr_duplicate_cleanup.md`(신규) · `hr/2_4_hr_overtime_r2.md`(삭제) · `hr/7_3_log_hr_r5.md`(삭제)
**영향**: 코드(index.html 등) 변경 없음 — 순수 문서 정리, 배포 리스크 없음

---

## 세션 요약 — 2026-07-22

**급여명세서 상여금/특별상여/기타수당 입력 커서 초기화 버그 수정 (r5, 대표님 지시로 테섭 생략 후 즉시 운영 배포)**

| 영역 | 버전 | 주요 변경 |
|------|------|-----------|
| 인사 급여명세서 | **2.6.1 → 2.6.2** (r5) | PC 작성 화면 `inp()` 헬퍼의 `oninput` → `onchange`로 변경. 상여금(초과근로)/특별상여/기타수당 입력 시 매 글자마다 포커스가 풀리던 문제 해결 |
| 백업 | v2.6.1 → **v2.6.2** | `backup/v2.6.2/hr_index_20260722.html` |

**계기**: 대표님 — "급여명세서에서 상여금, 특별상여, 기타수당 입력할 때 한 글자씩 입력해야 하는 번거로움이 있네." (스크린샷 첨부)
**원인**: 해당 3개 필드가 `oninput`으로 매 키 입력마다 우패널 전체를 재렌더링 → input DOM 교체로 포커스 소실. 같은 화면 다른 필드(기본급·4대보험·기숙사)는 이미 `onchange`라 문제 없었음
**배포 순서**: 대표님이 portal-test 생략, **production 직접 배포** 선택
**검증**: diff로 변경 1줄 확인, script 5블록 전체 node --check 문법 통과
**운영 커밋**: `hr/index.html` `9ad3f5d` / `index.html`(버전주석) `a8a300f`
**상세**: `8_8_log_payslip_input_fix.md`

---

## 세션 요약 — 2026-07-23 (r1)

**초과근로 결재 목록에 "본인 작성분만" 표시 필터 추가 (본섭 직접 배포)**

- 문제: `edoc/index.html`의 "초과근로 결재" 탭이 전 직원 상신 건을 필터 없이 모두 노출
- 수정: `renderOvertimeMain()`에 `authorUid` 일치 필터 추가 — 본인이 상신한 건만 표시
- 결재 처리는 기존대로 별도 **결재함** 탭에서 정상 동작 (영향 없음)
- 대표님 지시로 테스트서버 생략, production 직접 배포

**운영 커밋**: `edoc/index.html` `a6893b9` · `index.html`(버전주석) build 20260723
**백업**: `backup/v2.6.2/edoc/index.html`
**상세**: `8_8_log_overtime_own_only.md`


---

## 세션 요약 — 2026-07-23 (r2)

**금액 입력창 1000단위 콤마 포맷 적용 (전 모듈 공통 UI 컨벤션, 테섭 검증 → 대표님 확인 → 운영 배포 완료)**

| 영역 | 버전 | 주요 변경 |
|------|------|-----------|
| 기획(정산) | 1.3(견적) r3→**r4**, 1.4(정산) r1→**r2** | 견적 단가·금액·합계, 기술인력 단가, 직접입력 정산 단가·금액 — 입력창 6곳 `text`+콤마 포맷 전환 |
| 전자결재 | 3.3(문서) r1→**r2** | `DOC_CONFIG`에 `type:'money'` 신설 — 구매품의서 단가, 지출결의서 금액 입력창 콤마 포맷 + 저장 시 숫자 변환 + 상세보기 표시 개선 |
| 인사 급여명세서 | 2.5(급여) r5→**r6** | 지급내역·공제내역·상여금·특별상여·기타수당·기숙사공제·시급 — 입력창 9곳 콤마 포맷, 파싱을 `numClean()`으로 통일 |
| 백업 | v2.6.2 → **v2.6.3** | `backup/v2.6.3/gihoek/index.html`, `backup/v2.6.3/edoc/index.html`, `backup/v2.6.3/hr/index.html` |

**계기**: 대표님 지시 — "포탈 내 모든 '금액' 입력·출력은 1000단위마다 콤마(,) 표시" (Claude 메모리에 컨벤션으로 등록)
**조사**: 라이브 모듈 16개 전수 조사 → 출력은 대부분 `won()`으로 이미 처리돼 있었고, 문제는 `type="number"`라 콤마 표시가 불가능했던 입력창들이었음. 모바일(m/gihoek·m/hr·m/edoc)은 입력창이 없어 수정 불필요
**저장 안전성**: 기존 저장 로직이 콤마 안전 파서(`num()`)를 쓰고 있어 대부분 UI만 전환하면 됐음. `hr`는 일부 `Number(this.value)` 직접 파싱 구간이 있어 `numClean()`으로 교체(콤마 유입 시 `NaN` 방지)
**배포 순서**: portal-test 선배포(기존 test↔prod 코드 차이 보존을 위해 동일 패치를 test 원본에 개별 적용) → 대표님 확인("오케이") → production 배포
**검증**: 3개 파일 전체 `<script>` 블록 `node --check` 문법 통과, `fmtMoney`/`numClean` 파싱 로직 단위 테스트
**운영 커밋**: `gihoek/index.html`, `edoc/index.html`, `hr/index.html`, `index.html`(버전주석)
**상세**: `8_9_log_money_comma_format.md`

---

## 세션 요약 — 2026-07-24

**구매품의서/지출결의서 품목 다중입력 + 개별 링크 (테섭 검증 → 대표님 확인 → 운영 배포 완료)**

| 영역 | 버전 | 주요 변경 |
|------|------|-----------|
| 전자결재 | 3.3(문서) r2→**r3**, `edoc` 3.2r2→**3.3** | `DOC_CONFIG`에 신규 필드 타입 `items` 도입. 구매품의서(기존 단일 item/qty/unitPrice/refUrls → items로 통합), 지출결의서(items 신설). 품목명·수량·단가·링크를 한 행으로 묶어 다중 입력, 폼 하단에 가로 배치. 상세보기/A4 인쇄에 품목 표+합계 렌더링. 레거시 문서 하위호환(`getItemsForDisplay`) 처리 |
| 백업 | v2.6.3 → **v2.6.4** | `backup/v2.6.4/edoc/index.html` |

**계기**: 대표님 지시 — ① 지출결의서에 구매 링크 추가(기존 구매품의서 전용) ② 품의서·결의서 모두 구매 품목·수량 다중 입력 ③ 품목/수량/단가 하단 이동·가로 배치 ④ 품목별 개별 링크
**배포 순서**: 로컬 구현·jsdom 검증 → 대표님께 리스크 보고("바로 본섭 배포 시 문제는?" 질의에 답변) → 대표님 "테섭 보내" → portal-test 개별 패치 배포(테섭↔prod 기존 차이 보존) → 대표님 "확인했어, 본섭 보내도 되겠다" → production 배포
**검증**: `node --check` 전체 스크립트 문법 통과, jsdom 단위테스트(품목 행 추가/수집/자동계산/삭제/합계렌더/레거시 하위호환) 전항목 PASS
**운영 커밋**: `edoc/index.html`, `index.html`(버전주석)
**상세**: `8_10_log_edoc_items.md`

---

## 세션 요약 — 2026-07-24 (PJT 5.0.0)

**신규 PJT 자동편입 + 경량 PJT 워크스페이스 + 근로자 마스터 명부 + 월간 공수 집계 (대규모 신규 기능, portal-test 다회 검증 → 운영 배포 완료)**

| 영역 | 버전 | 주요 변경 |
|------|------|-----------|
| PJT | 4.5.2 → **5.0.0** | 신규 PJT 카드/사이드바 자동편입(`syncPjtRegistrySubtabs`), 경량 PJT 워크스페이스 신설(`pjt_light/`, 품목체크·근태공수·일정캘린더·공사일보, PC/모바일 반응형), 근로자 마스터 명부 신설(`master_workers`, 이름+생년월일 고유식별·팀장팀원 1단계·일당 관리자전용), 각 PJT 인원추가를 마스터 명부 선택 방식으로 전환, 월간 공수 집계 메뉴 신설(`pjt_manday/`, PJT별·개인별·팀별 합계, 개인별 통합 공수표 PNG저장, 마이그레이션 도구) |

**계기**: 대표님 지시 — ① 신규 등록 PJT도 P4 Ph2/Ph4처럼 카드+사이드바에 자동으로 뜨게 ② 경량 워크스페이스가 모바일 느낌이라 PC 대시보드로 ③ 일정도 캘린더로 ④ 여러 PJT 겸직 인원의 공수를 한 사람으로 통합 조회 ⑤ 팀장-팀원 구조로 팀별 공수·금액 집계

**주요 이슈 대응**:
- 경량 워크스페이스 레이아웃 3차 개선(모바일형 → 그리드 추가 → FAB 동일 PC 대시보드)까지 피드백 반영
- 캘린더 재렌더 시 빈 셀 누적으로 화면 밀리는 버그, 공수 저장이 출역체크에 종속되던 버그 수정
- **portal-test의 FAB/SUP가 프로덕션 Firebase를 그대로 참조하던 위험 발견 → 긴급 분리**
- 월간 공수 집계에서 SUP 데이터가 누락되는 문제를 디버그 패널로 추적한 결과, **FAB(`worker_manday`)와 SUP(`ph4_manday`)가 애초에 서로 다른 컬렉션을 쓰고 있었음**을 최종 확인·수정 (초기에 "공유 컬렉션"으로 잘못 가정했던 설계 오류)
- FAB/SUP 근태화면이 캐시변수(WORKERS) 미갱신으로 실제 명단과 어긋나던 버그 근본 수정
- 진단용 임시 디버그 뱃지는 **프로덕션 배포 전 완전 제거 확인**

**배포 순서**: 로컬 구현 → node --check 문법검증 + 단위테스트 → portal-test 개별 기능마다 배포·확인 반복(다회) → 대표님 "여기까지 본섭에 먼저 반영하자" → production 배포

**검증**: 전 파일 `<script>` 블록 `node --check` 통과, id/핸들러 교차검증, 진행률·공수합산·마이그레이션 그룹핑·URL결합 등 핵심 로직 단위테스트 통과

**운영 커밋**: `index.html`, `pjt/index.html`, `pjt_ph4/index.html`(수정), `pjt_light/index.html`, `pjt_manday/index.html`(신규)
**백업**: `backup/v5.0.0/`
**상세**: `7_10_log_pjt_5_0_0.md`, `8_11_log_2026-07-24_session.md`, `4_0_pjt_home.md`(갱신), `4_3_pjt_light.md`, `4_4_pjt_manday.md`(신규)

---

## 세션 요약 — 2026-07-24 (전자결재)

**전자결재 홈 하단 패널 4개 클릭 → 전체목록 모달 연결 (테섭 검증 → 대표님 확인 → 운영 배포 완료)**

| 영역 | 버전 | 주요 변경 |
|------|------|-----------|
| 전자결재 | 3.0(홈) r1→**r2** | 홈 하단 패널 4개(내가 결재해야 할 문서/수신함(회람)/내가 작성한 문서/게시된 문건) 헤더 클릭 시 상단 KPI 카드와 동일한 전체목록 모달(`edocShowList`) 연결. 기존엔 패널이 최신 8건 미리보기만 보여주고 클릭이 안 됐음 |
| 백업 | v2.6.4 → **v2.6.5** | `backup/v2.6.5/edoc/index.html` |

**계기**: 대표님(스크린샷 첨부) — "전자결재 홈에서, 하단에 카드 4개 모두 상세 게시판으로 이동할 수 있게 하면 좋겠어. 지금 기능으로는 게시된 문건도 저기에 리스트 뜨는 것들만 확인할 수 있잖아."
**배포 순서**: 로컬 구현·검증 → portal-test 개별 패치 배포(테섭↔prod 기존 차이 보존) → 대표님 "오케이 뜨긴 한다" → "이거 본섭에 반영해줘" → production 배포
**특이사항**: 배포 시점에 같은 날짜의 다른 세션(PJT 5.0.0)이 오래된 사본을 베이스로 `index.html` 버전주석을 덮어써 `edoc 3.3`→`3.2r1` 등으로 표기가 되돌아가 있던 것을 발견. 실제 코드는 손상 없음을 확인 후 PJT 5.0.0 변경 내역은 보존하고 모듈 버전 표기만 정정·병합. 세션로그 번호도 다른 세션과 `8.11`이 겹쳐 `8.12`로 재조정
**운영 커밋**: `edoc/index.html`, `index.html`(버전주석)
**상세**: `8_12_log_edoc_home_panel_click.md`

---

## 세션 요약 — 2026-07-24 (r1)

**초과근로 edoc↔hr 자동연동 누락 대응 — self-heal(자동 재동기화) 도입 (본섭 직접 배포)**

- 문제: 전자결재에서 승인한 초과근로 2건(이한영·정다애)이 인사에 미반영
- 원인 규명 시도: 코드 검토로는 정상, 진단 스크립트 작성했으나 대표님이 비개발자라 로컬 실행 불가, Chrome 확장도 미연결 → **직접 원인 규명 대신 자동 복구로 전환**
- 조치: `hr/index.html`에 `otSyncFromEdoc()` 추가 — 인사 초과근로 탭을 열 때마다 edoc 승인건 중 미반영분을 자동으로 채워 넣음 (사용자 조작 불필요, idempotent)
- 부수 발견(미해결): 결재함(`renderApproveBox`)의 `DOC_TYPES`에 `'overtime'` 누락 — 초과근로가 결재함에 노출되지 않음
- **사고**: 백업 경로(`backup/v2.6.3/hr/`) 버전 번호가 다른 세션과 충돌해 기존 백업을 실수로 덮어씀 → git blob API로 즉시 복구, 신규 백업은 `v2.6.4`로 재배치. 이후 백업 PUT 전 기존 파일 존재 확인 필수로 함

**운영 커밋**: `hr/index.html` `d6db6b0` · `index.html`(버전주석)
**백업**: `backup/v2.6.4/hr/index.html` (v2.6.3은 원상복구)
**상세**: `8_9_log_overtime_selfheal.md`
**미해결 과제**: edoc 결재함 `DOC_TYPES`에 `overtime` 추가 여부 결정 필요

---

## 세션 요약 — 2026-07-24 (r2)

**폴더블(Z Fold 등) 펼침 시 PC 화면 유지 (본섭 직접 배포)**

- 문제: `showApp()`이 User-Agent만으로 PC/모바일 판단 → 폴더블 펼쳐도 항상 모바일 화면으로 이동
- 수정: UA + 화면폭(900px) 동시 판단으로 변경. `localStorage` 기반 수동 PC/모바일 전환 버튼도 양쪽에 추가
- 데스크톱/일반 폰 사용자는 기존과 동일하게 동작 — "모바일 UA + 넓은 화면"(폴더블 펼침 등) 케이스만 새로 PC 유지

**운영 커밋**: `index.html` `65f33d8` · `m/home.html` `0623d31`
**백업**: `backup/v2.6.6/`
**상세**: `8_10_log_foldable.md`

---

## 세션 요약 — 2026-07-26 (PJT 5.1.0)

**FAB/SUP 종료·재개 확장 + 사이드바 "종료 PJT" 전용 메뉴 신설**

| 영역 | 버전 | 주요 변경 |
|------|------|-----------|
| PJT | 5.0.0 → **5.1.0** | 종료된 프로젝트 완전삭제 버튼 추가(관리자 전용·이중확인), `PJT_FIXED_BASE`로 P4 Ph2/Ph4 원본정의 보존 후 `pjt_settings.status` 기반 종료·재개 지원, 사이드바 PJT 하위 최하단에 `🗄️ 종료 PJT` 고정 메뉴 신설(FAB·SUP·경량PJT 종료 항목 통합 조회), 기존 PJT홈 하단 종료 섹션은 전용 뷰로 이전 |

**계기**: 대표님이 본섭에서 과거 테스트로 종료 처리된 화성17L 카드를 발견 → 완전삭제 기능 요청 → 이어서 "진행 중 프로젝트가 완결되면 워크스페이스 정리는 어떻게?" 질문 → 데이터 보존 위해 삭제보다 종료(아카이브) 권장, FAB/SUP엔 아직 종료 기능이 없던 점을 짚어 "미리 만들어두자" 지시 + "종료 PJT는 화면 하단이나 사이드바 새 폴더로" 지시

**주요 이슈**: 함수 삽입 중 `renderPjtHomeCards` 시그니처 줄을 실수로 삭제해 문법 오류 발생 → `node --check`로 즉시 발견·복구. 세션로그 번호(8.12)가 병렬 진행 중이던 다른 세션과 겹쳐 8.13으로 재조정

**배포 순서**: 로컬 구현 → node --check + 단위테스트(종료/재개 시 배열 복원 로직) → portal-test 배포·확인("잘 되네") → production 배포

**운영 커밋**: `index.html`
**백업**: `backup/v5.1.0/`
**상세**: `7_11_log_pjt_5_1_0.md`, `8_13_log_2026-07-26_session.md`, `4_0_pjt_home.md`(갱신)

---

## 세션 요약 — 2026-07-26 (r1)

**전자결재 "관리자 대신승인" 시 결재 미반영 — 진짜 근본 원인 발견·수정 (본섭 배포)**

- 07-24 self-heal에도 여전히 반영 안 되던 정다애·이한영 초과근로 건, 읽기전용 진단 페이지(`debug/overtime_check.html`)로 직접 데이터 확인 → **`edoc_overtime` 문서가 계속 `status:'pending'`(승인 자체가 안 됨)**임을 확인
- 원인: `docApprove()`가 관리자 대신승인 시 결재라인의 "내 차례"를 못 찾아 결재라인 갱신 실패 → 모든 문서타입에 공통되는 버그였음
- 수정: 순번 기반 단일 단계 타겟팅으로 재작성, 다단계 결재라인 동시승인 사고도 함께 방지
- 진단 페이지는 1차 Google 로그인 → ID/PW 로그인으로 수정(포털 실제 로그인 방식에 맞춤)

**운영 커밋**: `edoc/index.html` `4f0eace` · `index.html`(버전주석)
**백업**: `backup/v2.6.7/edoc/index.html`
**신규 도구**: `debug/overtime_check.html` (읽기 전용 진단 페이지)
**상세**: `8_11_log_approve_bug.md`
**다음 확인 필요**: 대표님이 대기중 4건 재승인 후 정상 반영 확인
**미해결 과제**: edoc 결재함 `DOC_TYPES`에 `overtime` 추가 여부 결정

---

## 세션 요약 — 2026-07-26 (r2)

**초과근로 결재목록 노출 회귀 수정 + 클릭 상세보기 모달 추가**

- 재승인 시도 중 발견: 07-23 "본인 작성분만 표시" 필터가 결재자/관리자의 결재 업무 자체를 막고 있던 회귀 → `renderDocList`와 동일한 `canView` 표준 패턴(관리자 전체·본인작성·결재라인 지정)으로 재수정
- 대표님이 재승인 후 데이터 정상 반영 확인
- 인사 → 초과근로 목록에서 사유가 말줄임으로 잘려 보이는 문제 → 행 클릭 시 사유 전문을 보여주는 상세보기 모달 추가 (PC 상세·모바일 직원별 조회·전체현황 상세 3곳)

**운영 커밋**: `edoc/index.html` `c82e787` · `hr/index.html` `28eccd6` · `index.html`(버전주석)
**백업**: `backup/v2.6.8/hr/index.html`
**상세**: `8_12_log_overtime_detail_modal.md`

---

## 세션 요약 — 2026-07-27

**pjt_manday 개인별 생년월일 — 근로자 마스터 명부와 자동 동기화**

| 영역 | 버전 | 주요 변경 |
|------|------|-----------|
| PJT (월간 공수) | 1.0.0 → **1.0.1** | `pjt_manday/index.html`의 개인별 합계가 프로젝트별 로컬 근로자 문서의 자체 `birth`/`name`을 읽던 것을, `master_workers`를 우선 조회해 **마스터 값으로 오버레이**하도록 수정. 상단 "생년월일 미입력" 경고도 마스터에도 없는 진짜 미등록자만 표시 |

**계기**: 대표님이 스크린샷 3장과 함께 신고 — "개인별 합계 생년월일 수정이 안 돼. 근로자 마스터 관리에서 입력한 게 자동으로 다 연동되면 좋겠는데. 상단에 미입력 인원도 남고." (김혁 1993-06-19 마스터 등록 vs 1993-06-01 화면표시, 미쳴·이미정·이승준·정대웅·민지환·태상원(→"태성원" 오타)이 마스터엔 있는데도 미입력 경고)

**원인**: 마스터 등록/수정(`saveMaster`)은 `master_workers` 컬렉션에만 쓰고, 각 프로젝트 로컬 근로자 문서(`pjt_workers_fab`/`pjt_workers_ph4`/`pjt_registry/{id}/workers`)의 자체 `birth`/`name` 필드는 갱신하지 않음 → 집계 화면이 그 로컬 필드를 읽고 있어 마스터 수정이 반영 안 됨

**배포 순서**: 대표님 지시 — 이번 건은 **portal-test 생략, production 직접 배포**

**운영 커밋**: `pjt_manday/index.html`
**백업**: `backup/v5.1.1/pjt_manday/index.html`
**상세**: `7_11_log_pjt_5_1_0.md`(v5.1.1 항목 추가), `8_14_log_2026-07-27_session.md`, `4_4_pjt_manday.md`(갱신)
**남은 과제**: 각 프로젝트 로컬 문서의 `name`/`birth` 필드 자체는 여전히 남아있음(표시만 오버레이) — 완전 정합성 원하면 "기존 인원 마이그레이션" 도구로 로컬ID→마스터ID 통합 필요. portal-test에는 아직 미반영

---

## 세션 요약 — 2026-07-27 (r2)

**월간 공수(pjt_manday) 메뉴 — 관리자 전용 접근권한 제한**

| 영역 | 버전 | 주요 변경 |
|------|------|-----------|
| 포털 셸 | index.html build 20260726c → **20260727** | `pjt_manday` 메뉴를 PC 사이드바·모바일 더보기 메뉴에서 관리자(`me.admin`)에게만 노출하도록 필터링, `openModule()`에 비관리자 접근 차단 가드 추가 |

**계기**: 대표님 지시 — "월간 공수 메뉴 접근 권한은 관리자 계정에만 줘."

**구현 위치**: `pjt_manday/index.html` 자체가 아니라 **포털 셸(`index.html`)** — PC 사이드바 서브메뉴 렌더링, 모바일 더보기 서브리스트 렌더링, `openModule()` 진입 가드 3곳

**배포 순서**: 대표님 지시("이것도 본섭에 바로 배포해") — portal-test 생략, production 직접 배포

**운영 커밋**: `index.html`
**백업**: `backup/v5.1.2/index.html`
**상세**: `7_11_log_pjt_5_1_0.md`(v5.1.2 항목 추가), `8_14_log_2026-07-27_session.md`(이어서 기록), `4_4_pjt_manday.md`(접근 권한 섹션 추가)
**남은 과제**: 제한은 포털 셸에서만 적용됨 — `pjt_manday/index.html`을 직접 딥링크로 접근하는 경로가 생기면 그쪽엔 가드가 없으므로 필요 시 앱 자체에도 권한 체크 추가 검토

---

## 세션 요약 — 2026-07-27 (r3)

**"우회 경로 없으면 좋겠는데" — 서브 메뉴 URL 직접 접근 보안 전수 점검·수정**

| 영역 | 버전 | 주요 변경 |
|------|------|-----------|
| pjt_manday | 1.0.1 → **1.0.2** | 페이지 자체에 로그인+관리자(`portal_users.admin`) 검증 추가. 검증 통과 전엔 어떤 Firestore 조회도 실행 안 됨. `?admin=1` 파라미터 기반 급여 노출 문제도 해결 |
| edoc | — | `?via=portal` 우회 버그 수정 — 승인·`perms.edoc` 확인 없이 입장되던 경로 차단 |
| gihoek | — | `?via=portal` 우회 버그 수정(**심각**) — 검증 없이 견적/정산/거래처/지출 전체가 즉시 구독·노출되던 경로 차단 |

**계기**: 대표님이 "지금 본섭에 있는 세부 메뉴들이 독립된 url로 접근할 수 있게 설계 되어 있나?"라고 질문 → 코드 확인 결과 모든 서브 메뉴가 독립 URL로 직접 접근 가능하고 `pjt_manday`는 자체 로그인 검증이 전혀 없다고 보고 → "우회 경로 없으면 좋겠는데, 방법이 없나?" → "응 본섭에 바로 반영하고, 모든 하위 메뉴들에 똑같이 적용해줘"

**전수 점검 결과**: `edoc`·`gihoek`은 자체 로그인 검증이 있었으나 `?via=portal`이면 검증을 건너뛰는 공통 버그 발견(특히 gihoek은 재무 데이터 전체가 무방비 노출). `pjt`·`pjt_ph4`·`pjt_light`·`hr`은 애초에 자체 로그인 검증이 없고, 데이터 로딩 진입점이 여러 곳에 흩어져 있어 pjt_manday처럼 단일 지점 차단이 어려움을 확인 — 회귀 위험 때문에 portal-test 선검증을 대표님께 제안, 다음 세션에서 진행 예정

**배포 순서**: 대표님 지시로 pjt_manday·edoc·gihoek 3건 모두 portal-test 생략, production 직접 배포

**운영 커밋**: `pjt_manday/index.html`, `edoc/index.html`, `gihoek/index.html`
**백업**: `backup/v5.1.3/pjt_manday/index.html`, `backup/v5.1.4/edoc/index.html`, `backup/v5.1.4/gihoek/index.html`
**상세**: `7_11_log_pjt_5_1_0.md`(v5.1.3), `7_4b_log_edoc.md`, `7_2b_log_gihoek_security.md`(신설), `8_14_log_2026-07-27_session.md`, `4_4_pjt_manday.md`·`3_0_edoc_home_approve.md`·`1_0_gihoek_home_r2.md`(각 갱신)
**남은 과제**: `pjt`·`pjt_ph4`·`pjt_light`·`hr` 4개 파일에 동일한 접근검증 적용 필요 — portal-test 선검증 후 진행 예정. Firestore 보안 규칙(콘솔) 자체 점검도 권장(프론트엔드 검증은 1차 방어선일 뿐, 최종 방어선은 보안 규칙)

---

## 세션 요약 — 2026-07-27 (r4, 최종) — FAB/SUP/경량PJT/HR 접근검증, portal-test 선검증 후 본섭 배포

| 영역 | 버전 | 주요 변경 |
|------|------|-----------|
| pjt (FAB) | — | 접근검증 게이트 추가. `waitForFirebaseAndInit()` 폴링 조건에 `window._accessGranted` 편입해 전체 초기화 캐스케이드를 단일 지점에서 차단, 그 외 흩어진 preload·구독 4곳도 개별 게이트 |
| pjt_ph4 (SUP) | — | pjt와 동일 패턴 적용 (컬렉션명만 ph4 접미사) |
| pjt_light (경량PJT) | — | pjt_manday와 같은 단일 진입점(`init()`) 구조라 깔끔히 처리. `?admin=1` 기반 관리자 판단도 실제 검증으로 전환 |
| hr (인사) | — | **가장 심각** — `?via=portal` 파라미터 존재만으로 통과(Firebase Auth 자체 없음)하던 걸 실제 로그인+`portal_users`(`perms.hr`) 검증으로 교체. 급여·계약서 등 인사 데이터 전체가 무방비였던 상태 해소 |

**진행 방식**: 지난 라운드에서 회귀 위험을 이유로 대표님께 "portal-test 선검증 후 본섭 배포"를 제안 → 대표님 선택. firebaseConfig만 테섭용으로 바꿔 동일 패치를 portal-test에도 배포 → 대표님이 정상 로그인 접근과 시크릿창 직접 URL 접근(차단 확인) 양쪽 다 테스트해서 확인("ㅇㅋㅇㅋ 전부 다 된다") → 본섭 배포 진행. 테섭의 pjt_manday도 구버전(v1.0.0)이었던 걸 발견해 본섭과 동일하게 맞춤

**운영 커밋**: `pjt/index.html`, `pjt_ph4/index.html`, `pjt_light/index.html`, `hr/index.html`
**백업**: `backup/v5.1.5/{pjt,pjt_ph4,pjt_light,hr}/index.html`
**상세**: `7_11_log_pjt_5_1_0.md`(v5.1.5), `8_14_log_2026-07-27_session.md`, `4_1_pjt_fab.md`·`4_2_pjt_sup.md`·`4_3_pjt_light.md`·`2_0_hr_home_r3.md`(각 갱신 — hr 문서는 과거 "Firebase Auth 완전 제거" 설계 서술을 정정)

**세션 전체 요약**: 이번 세션 하루 동안 총 4차례 배포 라운드 진행 — ① pjt_manday 생년월일 마스터 동기화 ② 월간공수 메뉴 관리자 전용 제한(포털 셸) ③ pjt_manday 자체 접근검증 + edoc/gihoek via=portal 우회 버그 수정 ④ pjt/pjt_ph4/pjt_light/hr 접근검증 게이트. 모든 서브 메뉴에 프론트엔드 레벨 접근검증 구축 완료.

**남은 과제**: ~~Firestore 보안 규칙(콘솔) 자체 점검~~ → 완료됨. 아래 최종 요약 참고

---

## 세션 요약 — 2026-07-27 (r5, 세션 최종) — 3중 방어 완성 (화면단 + 로그인방식 + Firestore 규칙)

**계기**: 대표님 — "외부 유입이 완전히 단절된 폐쇄적인 workspace를 원해 나는" → 범위 확인 후 "계정 가입/로그인 차단 + Firestore 규칙" 둘 다 선택

| 레이어 | 내용 | 담당 |
|--------|------|------|
| ① 화면(UI)단 | 전 서브 메뉴 로그인+승인+권한 검증 게이트 (이번 세션 1~4라운드에서 완료) | 코드 배포 (Claude) |
| ② 로그인 방식 | Firebase Auth를 이메일/비밀번호만 허용하도록 콘솔에서 제한. 관리자만 계정 생성·`@jhsol.kr` 도메인 고정은 기존에 이미 구현돼 있었음을 재확인 | Firebase 콘솔 (대표님 직접) |
| ③ 데이터단(Firestore 규칙) | 승인된 `@jhsol.kr` 계정만 전체 컬렉션 읽기/쓰기 가능하도록 규칙 게시. `portal_users`는 본인만 읽기+관리자만 쓰기 | Firebase 콘솔 (대표님 직접, 규칙 초안은 Claude 작성) |

**진행**: Claude가 Firestore 규칙 초안(`firestore_보안규칙_적용가이드.md`)과 Firebase 콘솔 직접 링크(Authentication/Sign-in method, Firestore/Rules, 본섭·테섭 각각)를 제공 → 대표님이 콘솔에서 직접 로그인 방법 제한 + 규칙 게시(테섭·본섭 둘 다) → 정상 로그인·데이터 조회 확인("데이터 잘 보이는 것 같은데")으로 마무리

**Claude가 직접 할 수 없었던 이유**: Claude 환경은 `googleapis.com` 접근이 차단돼 있어 Firebase 콘솔·API에 직접 접근 불가 — 규칙 초안 작성과 안내까지만 가능, 실제 게시는 대표님이 콘솔에서 직접 수행

**상세**: `0_4_structure_index.md`(보안 아키텍처 3중 방어 섹션 신설), `8_14_log_2026-07-27_session.md`(세션 최종 마무리 기록)

---

## 오늘(2026-07-27) 하루 전체 요약

한 세션 안에서 5차례 라운드에 걸쳐 포털 전체의 보안을 프론트엔드부터 인프라까지 재정비:
1. pjt_manday 생년월일 마스터 동기화
2. 월간공수 메뉴 관리자 전용 제한
3. pjt_manday 자체 접근검증 + edoc/gihoek via=portal 우회 버그 수정
4. pjt/pjt_ph4/pjt_light/hr 접근검증 게이트 (portal-test 선검증 후 본섭)
5. Firebase 콘솔 — 로그인 방식 제한 + Firestore 보안 규칙 게시

결과: 화면단(코드) + 로그인 방식(Auth 설정) + 데이터단(Firestore 규칙) 3중으로 막혀, 승인된 `@jhsol.kr` 계정이 아니면 어떤 경로로도(정상 화면, URL 직접 접근, 브라우저 개발자도구를 통한 Firestore SDK 직접 호출 포함) 데이터에 접근할 수 없는 구조 완성.

---

## 세션 요약 — 2026-07-27 (r6) — pjt_manday 팀별 합계, 팀 단가 방식으로 변경

| 영역 | 버전 | 주요 변경 |
|------|------|-----------|
| pjt_manday | 1.0.2 → **1.1.0** | 팀별 합계 금액을 "팀원별 개인단가×공수 합산" 대신 **팀 단가(`teamRate`) × 팀 총공수**로 계산. 관리자용 ✏️ 편집 버튼으로 팀장 문서에 `teamRate` 저장. 팀원 개인별 금액 표시 제거 |

**계기**: 대표님 — "개인별 단가 말고 팀 단가로 계산할 수 있게 해줘. 근로자 개개인한테 얼마 나가는 지는 나한테 중요한 정보가 아니야" (팀별 합계 화면 스크린샷 첨부)

**영향 범위**: 팀별 합계 섹션에만 적용, 개인별 합계(상단) 섹션은 기존 개인별 `dailyRate` 방식 그대로 유지

**운영 커밋**: `pjt_manday/index.html`
**백업**: `backup/v5.1.6/pjt_manday/index.html`
**상세**: `4_4_pjt_manday.md`(팀별 합계 섹션 갱신), `7_11_log_pjt_5_1_0.md`(v1.1.0 항목 — 200줄 임박, 다음 항목부터 분할 필요), `8_14_log_2026-07-27_session.md`

---

## 세션 요약 — 2026-07-27 (r7, 세션 최종) — pjt_manday 마스터 목록 개인별 단가 표시 제거

| 영역 | 버전 | 주요 변경 |
|------|------|-----------|
| pjt_manday | 1.1.0 → **1.1.1** | 근로자 마스터 관리 목록에서 개인별 단가(`dailyRate`) 표시 제거. 이름·생년월일·소속·수정/삭제 버튼만 남김. 필드·입력폼 자체는 유지 |

**계기**: 대표님 — "여기에 개인별 단가는 없어도 돼" (마스터 목록 스크린샷 첨부, v1.1.0 팀 단가 전환에 이은 후속 정리)

**운영 커밋**: `pjt_manday/index.html`
**백업**: `backup/v5.1.7/pjt_manday/index.html`
**상세**: `4_4_pjt_manday.md`(근로자 마스터 명부 섹션 갱신), `7_11b_log_pjt_5_1_0.md`(신설, 7_11 200줄 분할)

---

## 2026-07-27 하루 전체 최종 요약 — 총 7차례 배포 라운드

1. pjt_manday 생년월일 마스터 동기화 (v1.0.1)
2. 월간공수 메뉴 관리자 전용 제한 (포털 셸)
3. pjt_manday 자체 접근검증 (v1.0.2) + edoc/gihoek via=portal 우회 버그 수정
4. pjt/pjt_ph4/pjt_light/hr 접근검증 게이트 (portal-test 선검증 후 본섭)
5. Firebase 콘솔 — 로그인 방식 제한 + Firestore 보안 규칙 게시 (3중 방어 완성)
6. pjt_manday 팀별 합계 → 팀 단가 방식 (v1.1.0)
7. pjt_manday 마스터 목록 개인별 단가 표시 제거 (v1.1.1)

하루 만에 포털 전체 보안 아키텍처 재정비(화면단·로그인방식·데이터단 3중 방어) + pjt_manday 급여 표시 방식을 팀 단위로 전면 전환 완료.

**추가**: 위 7개 라운드가 개별 서브 앱 파일에는 반영됐지만 메인 `index.html` 상단 버전 코멘트엔 누락돼 있던 것을 대표님이 지적("잉 당연히 반영 해야하는 거 아니야?") → build `20260727`→`20260727b`로 오늘 하루 전체 요약을 반영. 백업: `backup/v5.1.8/index.html`. 향후 여러 서브 앱에 걸친 배포일에는 세션 마무리 시점에 메인 버전 코멘트 갱신을 체크리스트 항목으로 명시할 것.

---

## 2026-07-28 세션 요약 — 모바일 PJT 강조 작업 중 permission-denied 전면 장애 발견·수정

**시작 목표**: 모바일 홈에서 PJT 관리 강조(기획/인사 축소), PJT 관리 데이터를 PC 기준 실시간 연동.

1. `m/home.html`: PJT(FAB/SUP) 개별 강조 카드, 전자결재 하단이동, 기획/인사 축소
2. `m/pjt.html`: `pjt_registry` 실시간 구독 추가 → 경량PJT(화성 17L 등)가 모바일에도 자동 노출, 경량PJT 상세뷰(일정/근태·공수) 신설
3. **[긴급 대응]** 테섭에서 PJT 진입 시 `permission-denied` 재현 → 한때 본섭 로딩 자체 불가 상황까지 발생(별건, 브라우저 로컬 캐시/보안예외 문제로 확인·해결) → 최종적으로 **모바일 페이지 전체(pjt·hr·edoc·gihoek·account·admin)에 Firebase Auth 초기화가 아예 빠져있던 것**이 근본 원인으로 확인. 전날(7/27) Firestore 규칙이 `request.auth` 필수로 강화되며 잠재됐던 버그가 전면 노출된 것. 6개 파일 전부 `getAuth()`+`onAuthStateChanged()` 부트스트랩 추가하여 수정
4. Firestore 규칙에 `portal_users` 최초 관리자 자가부트스트랩 예외 추가 (테섭 기준, 재발방지용)

**배포**: 테섭 선검증 → 본섭 7개 파일(pjt/home/hr/edoc/gihoek/account/admin) + index.html 버전코멘트(`20260728a`, PJT 5.2.0/mobile 5.3.0). 백업: `backup/v5.2.0/m/`

**교훈**: Firestore 보안 규칙을 강화할 때는 그 규칙에 의존하는 모든 클라이언트(PC뿐 아니라 모바일 등 별도 진입점)가 실제로 인증 토큰을 실어 보내는지 함께 점검해야 한다. 규칙 배포 자체는 무해해 보여도, 클라이언트 쪽 Auth 초기화가 빠져있으면 조용히 전면 장애로 이어질 수 있음.

**보류**: ③ 월간 공수(pjt_manday) 모바일 뷰 — 다음 세션 착수 예정. 경량PJT 모바일의 업무지시·보고/공정 탭도 미지원.
