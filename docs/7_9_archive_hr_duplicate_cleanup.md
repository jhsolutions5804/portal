# 7.9. 아카이브 — hr/ 폴더 중복 문서 정리 (2026-07-21)

> `hr/2_4_hr_overtime_r2.md`, `hr/7_3_log_hr_r5.md` 삭제 전 고유 내용 백업
> 삭제 사유: `docs/` 폴더 중앙집중 문서 체계로 넘어가기 전(2026-06-26~27) 코드 옆에 임시로 만들어둔 사본으로,
> 이후 `docs/2_4_hr_overtime_r2.md`(2026-07-06 신규 생성) · `docs/7_3_log_hr_r10a/r10b.md`(2026-06-29 분할 생성)로
> 각각 대체되어 더 이상 갱신되지 않고 방치되어 있었음. 상세 추적 경위는 세션로그 참조.
> 작성: 춘식이(Claude)

---

## A. `hr/2_4_hr_overtime_r2.md`에만 있던 내용 — 초과근로 PC 레이아웃 (2026-06-27, F1)

> 현재 `docs/2_4_hr_overtime_r2.md`(r3)에는 이 다이어그램/함수표가 없음. 코드(`hr/index.html`)에는
> 여전히 해당 함수들이 그대로 존재하므로 기능상 문제는 없고, 참고용 구조 설명만 archive.

### PC 뷰 구성

```
┌─────────────────────────────────────────────────────────┐
│  ⏱ 초과근로          ◀ 2026년 06월 ▶                    │  ← 헤더
├──────────┬──────────┬──────────┬──────────────────────────┤
│ 총 시간  │ 총 수당  │  인원   │  건수                    │  ← KPI 바
├──────────┴──────────┴──────────┴──────────────────────────┤
│ 직원 목록  │                                              │
│ ─────────  │  (선택 직원) 상세 내역                      │
│ 홍길동 대리│  · 통상임금 정보                            │
│ 김철수 사원│  · 누계 시간 / 누계 수당 KPI               │
│ 이영희 과장│  · 날짜별 상세 테이블 (수정·삭제, 사유 컬럼)│
│           │                                              │
│ ─────────  │                                              │
│ ➕ 입력폼 │                                              │
│ 날짜      │                                              │
│ 직원 선택 │                                              │
│ 통상임금  │                                              │
│ 시간(h)   │                                              │
│ 사유(선택)│                                              │
│ 예상수당  │                                              │
│ [저장]    │                                              │
│ [🗑초기화]│                                              │
└───────────┴──────────────────────────────────────────────┘
```

### 주요 함수

| 함수 | 역할 |
|------|------|
| `renderOvertimeMain()` | PC/모바일 분기 진입점 |
| `renderOvertimePC()` | PC 뷰 뼈대 렌더 + `otPcInit()` 호출 |
| `renderOvertimeMobile()` | 기존 모바일 카드 뷰 (변경 없음) |
| `otPcInit()` | 직원 목록 + select 초기화 + KPI 로드 |
| `otPcLoadKpiAndHours(workers)` | 월별 KPI 집계 + 직원별 시간 업데이트 |
| `otPcChangeMonth(delta)` | 월 변경 → KPI + 우패널 갱신 |
| `otPcSelectWorker(id, name, rank)` | 직원 클릭 → active 처리 + select 동기화 + 우패널 갱신 |
| `otPcShowPersonDetail(id, name, rank)` | 우패널: 통상임금·누계·날짜별 상세 |
| `otPcCalcPreview()` | 입력폼 통상임금 미리보기 |
| `otPcSave()` | 저장 후 KPI·우패널 갱신 |
| `otPcDelete(docId, ...)` | 삭제 후 KPI·우패널 갱신 |
| `otPcEdit(docId, ...)` | `renderOvertimeEdit(... 'pc')` 호출 |
| `otUpdate(docId, backType)` | `backType==='pc'` 분기 추가 — 저장 후 `renderOvertimeMain()` |

### 직원 선택 ↔ 입력폼 select 동기화

- 직원 목록 클릭 → `otPcSelectWorker()` → select도 해당 직원으로 동기화
- 입력폼 select 변경 → `otPcCalcPreview()` → 직원 목록 active도 동기화

---

## B. `hr/7_3_log_hr_r5.md`에만 있던 내용 — 초기 커밋 이력 (2026-06-24 ~ 06-27)

> `docs/7_3_log_hr_r10a.md`는 2026-06-29부터 시작하므로, 그 이전 구간(hr 앱 최초 구축~F1)은
> 이 아카이브가 유일한 기록임.

### 커밋 이력

| SHA | 날짜 | 내용 |
|-----|------|------|
| *(초기)* | 2026-06-24 | 7개 탭: 홈·명부·근로계약·연봉계약·초과근로·급여·퇴직금 |
| *(Rev2)* | 2026-06-25 | 연차 현황 탭(8번) 추가 |
| `2cf3c6996b` | 2026-06-26 | Firebase config 원본 복원 |
| `af97588c` | 2026-06-26 | 인사 홈 hrShowList 팝업 |
| `6b3d929611` | 2026-06-26 | 인사 홈 연차카드 빈칸 수정 |
| `9eebe9bad5` | 2026-06-26 | 근로자 목록 사번순 정렬 |
| `1f9d685b86` | 2026-06-26 | B1 연차상세 모달 + B2 관리자 연차 CRUD |
| `91adcb481b` | 2026-06-26 | B4 갑 서명/도장 등록·출력 |
| *(미배포)* | 2026-06-26 | C1 직접 접근 인증 portal_users 통일 |
| `668d253f` | 2026-06-27 | [D1] hr 내부 사이드바 완전 제거 |
| `1fcb4a58` | 2026-06-27 | [D3] 근로계약서 탭 PC 테이블 레이아웃 |
| *(2026-06-27)* | 2026-06-27 | [E1] 사이드바 연봉계약서 번호 2→3, 연봉계약서 PC 테이블 레이아웃, 사원번호 순 정렬 |
| `88b377aa` | 2026-06-27 | [F1] 초과근로 PC 레이아웃 (B안) |

### 주요 변경 상세

**Firebase config 오염 복원**
- 오염값: `messagingSenderId: '369463179140'` (0 추가) / 원본: `'36946317914'` (11자리) — hr + edoc 양쪽 복원

**인사 홈 연차카드 빈칸 수정 (A3)**: 7번 카드가 `full`(한 줄 전체)로 돼 6번 카드 옆이 비었음 → 7번을 일반 카드로 변경, 6·7번 나란히 배치

**근로자 목록 사번순 정렬 (B3)**: 6곳 전체에 `empNo` 기준 정렬 적용

**연차 상세 모달 (B1)**: 행 클릭 → `showLeaveDetail(wid, wname, hireDate)` 모달, 요약 카드 + 사용 내역

**관리자 연차 CRUD (B2)**: "연차 등록" 버튼(관리자만), `edoc_leave`에 `adminCreated:true, status:'posted'`로 저장

**갑 서명/도장 (B4)**: `company_settings/signatures.kjh` base64 저장, 근로계약서·연봉계약서 A4 출력 시 자동 삽입

**직접 접근 인증 통일 (C1)**: `allowed_users/{email}` → `portal_users/{uid}` 기반으로 통일

**hr 내부 사이드바 완전 제거 (D1)**: 포털 메인 사이드바와 중복 → `<aside id="hr-sidebar">` HTML·CSS 전체 삭제

**근로계약서 탭 PC 테이블 레이아웃 (D3)**: 카드 선택 화면 → 근로자 명부와 동일한 PC 테이블 직접 표시로 전환

**연봉계약서 개편 (E1)**
- 사이드바 번호: 연봉계약서 2→3, 초과근로 3→4, 급여명세서 4→5, 퇴직금 정산 5→6, 연차 현황 6→7
- PC 테이블 레이아웃 전환 (max-width 840px→1100px), 직원 목록 사원번호 순 정렬

**초과근로 PC 레이아웃 (F1)**: 카드 3개 선택 방식 → `window.innerWidth>=900` PC/모바일 자동 분기 단일 화면으로 전환 (상세는 위 A절 참조)

### 이슈 & 해결

| 이슈 | 해결 |
|------|------|
| Firebase config messagingSenderId 0 추가로 Auth 오류 | 원본 11자리로 복원 |
| renderHome 템플릿 리터럴 내 백틱 중첩 → SyntaxError | DOM 직접 조작 방식으로 우회 |
| 직접 URL 접근 시 allowed_users 미존재로 접근 불가 | portal_users 기반으로 인증 통일 |
| hr 앱 내부 사이드바 포털과 중복 표시 | 사이드바 HTML·CSS 완전 제거 |
| 근로계약서 탭 카드 선택 방식 — 2단계 진입 비효율 | PC 테이블 직접 표시로 교체 |
| 연봉계약서·근로계약서 사이드바 번호 중복(둘 다 2) | 연봉계약서 3으로 변경, 이후 번호 연쇄 수정 |
| 연봉계약서 직원 목록 정렬 기준 없음 | empNo 오름차순 정렬 적용 |
| 초과근로 탭 뷰 전환마다 Firestore 재조회 비효율 | PC 단일 화면 — 직원 선택·월 변경 시에만 재조회 |
| otUpdate backType 오버라이드 순서 문제 (선언 전 캡처 시도) | 원본 otUpdate에 'pc' 분기 직접 추가로 해결 |

---

## 삭제된 원본 파일

- `hr/2_4_hr_overtime_r2.md` (생성: `ef5ba294`, 2026-06-26)
- `hr/7_3_log_hr_r5.md` (생성: `60a2c3a6`, 2026-06-26)
