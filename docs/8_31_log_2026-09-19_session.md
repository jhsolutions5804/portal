# 세션 로그 — 2026-09-19 — 모바일 조직도 정렬

> 작성: 춘식이(Claude)

## 요청

대표님 — 모바일 환경 검토 착수. 첫 항목으로 조직도(`m/admin.html`)가 두서없이 나온다며 정렬 변경 요청. 1차 "사번 순" → 2차 세부 규칙 확정(송지훈 최상단·부서 지정순서·부서 내 사번순).

## 구현

`renderOrg()` 재작성 — 대표 분리(`isTop`), 부서 고정순서(`ORG_DEPT_ORDER`), 사번 numeric 정렬(`cmpEmp`, 사번 없음은 맨 뒤). 상세: `7_39_log_mobile_org_sort.md`.

## 배포

대표님 지시("둘 다 해") 따라 production + portal-test `m/admin.html` 동시 배포.

1. 로컬 검증: node --check(JS)·CSS 중괄호·시뮬레이션 통과, 두 레포 파일 diff가 Firebase 설정 외 동일함 확인 후 독립 적용
2. 백업: `backup/v2.7.0/m/admin.html` (양쪽, 경로 충돌 없음 확인)
3. 배포: `m/admin.html` (양쪽), 버전 → 모바일 관리 2.7.0
4. 문서 갱신: `5_org_chart.md`(모바일 조직도 섹션·최종 개정), `8_0_mobile_r2.md`(m/admin.html 행·최종 수정)

## 이어서 — 종료 PJT 모바일 반영

대표님 — SUP 종료 처리했는데 모바일에만 노출됨. 신규/종료 PJT만 정확히 나오면 되고 순서는 무관. `m/home.html`·`m/pjt.html`·`m/schedule.html`에서 `pjt_settings/{p4ph2|p4ph4}.status==='ended'`인 고정 PJT를 제외(실시간). 상세: `7_40_log_mobile_ended_pjt_hide.md`.

배포: 대표님 지시("큰 건 아니니까 둘 다 동시에 해") 따라 production + portal-test 동시. 모바일 홈 2.7.1 / PJT 5.3.0 / 오늘 일정 1.0.1, 백업 `backup/v2.7.1/m/home.html`·`backup/v5.3.0/m/pjt.html`·`backup/v1.0.1/m/schedule.html`(양쪽). 문서: `8_0_mobile_r2.md` 갱신.

## 이어서 — 모바일 통합 일정 작성/수정/완료체크

대표님 — 오늘 일정 화면에서 PC처럼 작성/수정/완료 체크 요청. `m/schedule.html`에 등록·수정·삭제·완료체크·할 일 추가(PC 홈 일정과 동일 스키마, PJT 권한자·관리자만). 상세: `7_41_log_mobile_schedule_crud.md`.

배포: 대표님 지시("둘 다")로 production + portal-test 동시. 일정 1.1.0, 백업 `backup/v1.1.0/m/schedule.html`(양쪽). 문서: `8_0_mobile_r2.md`, `4_0_pjt_home.md` 갱신.

## 남은 이슈

- 모바일 검토 계속 진행 예정(다음 화면은 대표님 지시 대기). 방향: 현재 진행 중인 일상 업무 강조.
- 모바일 FAB/SUP 카드 이름·설명은 `pjt_settings` 값과 동기화하지 않음(코드 고정 문구).
- `m/pjt.html` 현장별 일정 폼은 PC와 항목이 다름(메모 중심·완료 필드 `doneAt`) — 통합 일정과 동일하게 맞출지 대표님 결정 대기.
- 통합 일정 화면은 브라우저 렌더링 미확인 상태로 배포 — 실기기 확인 필요.
- PC 조직도와 모바일 조직도의 부서 순서 불일치(의도적으로 미변경).
