# 인사 저장 서류 양식 동결 — 근로계약서·연봉계약서·급여명세서 (2026-09-19)

> 작성: 춘식이(Claude) · hr v2.4.1 · 기능 문서: `2_9_hr_saved_doc_freeze.md`

## 요청
대표님 — 포괄→비포괄 전환·급여명세서 항목 변경 이후, 기존에 작성 완료한 연봉계약서(연봉 32,285,088원인데 월급여 합계 2,152,339원으로 출력) 등이 함께 바뀜. "직원이 서명하고 저장한 서류는 어떠한 경우에도 수정되면 안 됨. 시스템 변경은 유지하고, 변경 이전 서류는 원복. 범위: 근로계약서·연봉계약서·급여명세서."

## 원인
저장 문서엔 입력값만 있고, 조회/PDF 출력이 매번 현재 양식·현재 `calcAnnualSalary`로 재렌더. 변경 지점: `c043449`(8/29 계산식 0원화), `260c91b`(9/1 조항·항목 추가), `91049d0`(9/2 `wageType` 기본 비포괄 문구). 급여명세서 조회(`psViewDetail`)는 저장값 일부만 `ps`에 복원 후 `psCalcAll()` 재계산 → 기술수당·여비교통비·휴일근로수당이 저장값과 달라질 수 있었음(9/2 이후 문서 포함).

## 수정 (`hr/index.html` 2.4.0 → 2.4.1)
1. git 이력(`c043449^`·`260c91b^`·`91049d0^`·HEAD)에서 시대별 렌더러를 추출해 **동결 렌더러**로 삽입(연봉 4벌·근로 2벌·급여명세서 4벌, 동결 계산식 2벌). 기계 변환(`window.open/write`→`return`, 전역 상태→인자).
2. `docTplVer(d)`(tplVer→savedAt→v0)로 버전 판정, `docHtmlByVer`로 출력. 저장 시 `tplVer:'v3'` 기록.
3. 급여명세서 조회는 저장값 그대로(`__psSavedC`) — 재계산 제거. `psViewDetail` 2벌·`annualPreviewSaved2`·`laborPreviewSaved2` 교체.
4. 작성 화면 미리보기·신규 양식은 변경 없음. Firestore 데이터 변경 없음.

## 검증
- 시대별 원본 vs 동결 출력 32건 일치, 경계 판정, 진입점 dispatch 테스트(전역 `ps` 미변경, 저장값 그대로 표시), `node --check` 전 스크립트 통과.
- 본섭 사례(시급 10,320 사무직)로 v0 출력 재현: 합계 2,690,424원 ×12 = 32,285,088원.
- production ↔ portal-test 파일 diff = Firebase 설정 6줄만 다름.
- 제약: 실행 환경에서 Firestore 미접속·브라우저 없음 → 실제 문서 렌더링은 배포 후 확인(테섭은 대표님 확인 완료).

## 배포
- portal-test: `dc3268d`(hr), 백업 `backup/v2.4.1/hr/index.html` → 대표님 확인("제대로 나온다").
- production: `1008a4d`(hr), 백업 `backup/v2.4.1/hr/index.html`(경로 충돌 없음 확인). 배포 전 본섭 hr 원격 == 작업 기준본 재확인(그 사이 모바일 커밋만 있었고 hr 무변경).
- 동일 패치를 각 레포 파일에 독립 적용(파일 통째 복사 아님).

## 이슈 & 참고
- 갑 도장 이미지는 현재본 표시, 급여명세서 입사일·생년월일 `-` — 한계로 `2_9`에 기록.
- 양식 변경 시 동결 렌더러 수정 금지, 새 vN 추가(절차는 `2_9`).
